# 📜 Gendrive v1.0.0 リリースノート
**【 Personal Action Engine 正式統合版 ・ バージョン表示・リリースノート機能導入 】**  
*発行日時: 2026-08-29*

---

## 1. 🔬 事前コード分析・設計・リファクタリング判断

### 背景と目的
* **背景**:
  * Gendrive（旧habit-app）は、セクション管理、フォーカスモード、デイリー俯瞰、マスター一括編集、ビジョン管理、単独タイマーの6大画面、GASクラウド同期、Google Driveタイムマシーン復元、TaskChute式ETA計算など、日常運用に耐えうるすべてのコア機能が完成。
  * これまでアプリ内にバージョン表示がなく、更新履歴（リリースノート）の仕組みも存在しなかったため、Clipperと同様の更新管理体系を整備することとした。
* **課題と解決方針**:
  * GendriveはPC版において主にChromeブラウザやPWAウィンドウ内で動作するため、Clipperのような独立したタイトルバーやWindowsメニューバーが存在しない。
  * ヘッダー左側の `[⚡ Gendrive ACTION ENGINE]` と `[ ◀ 2026年8月29日(土) 今日 ▶ ]` の間の余白を活用し、`[ v1.0.0 ]` のピル型バッジを配置。
  * バッジをクリックすることで、Webアプリ内で直接「リリースノート・更新履歴モーダル」を閲覧できる仕組みを構築。
  * `config.js` に `APP_VERSION = 'v1.0.0'` を定義し、Single Source of Truth（一元管理）を徹底。

---

## 2. 💡 「知ると面白い、今回の技術の仕組み」解説（エンジニア視点・ユーザー視点）

### 💡 秘訣①: Single Source of Truth（唯一の信頼できる情報源）
* **仕組み**: バージョン文字列（`"v1.0.0"`）をHTMLやJS各所に直書き（ハードコード）せず、`config.js` の `APP_VERSION` 定数に一元集約。
* **効果**: 次回以降のアップデート時、`config.js` を1行変更するだけで、ヘッダーバッジ・モーダル内タイトル・ログ出力など全画面の表記が連動して切り替わります。

### 💡 秘訣②: Webアプリにおける「情報階層（Info Hierarchy）」の最適化
* **配置の意図**: バージョン表示は頻繁に操作するものではないが、トラブルシューティングや新機能把握時には必須。プライマリアクション（タスク開始・日付移動）を邪魔せず、かつ視界に入る絶妙な位置（ヘッダーロゴ直後）に配置しました。

### 💡 秘訣③: Clipper互換の Markdown / Web Modal ハイブリッド管理
* **開発者視点**: プロジェクトルートの `CHANGELOG.md` や `docs/release_notes/` にMarkdown形式で詳細な技術レポートを蓄積。
* **ユーザー視点**: アプリヘッダーのバッジをクリックするだけで、CSSスタイリングされた美しいモーダルで更新内容を直感的に把握可能。

---

## 3. 🌟 Gendrive v1.0.0 の主要機能・変更点（全機能一覧）

### 📌 バージョン管理・リリースノート機能（新規）
* **ヘッダー・バージョンバッジ**: ヘッダー左側に `v1.0.0` バッジを新設。
* **リリースノート閲覧モーダル**: バッジクリックで更新履歴・機能一覧を閲覧可能。

### 📌 Gendrive 6大メイン画面（統合機能）
1. **セクション画面 (`1`)**: 1日を時間帯（第1セクション〜第4セクション等）に区切った時間割ビュー。ETA計算・セクション経過バー表示。
2. **フォーカス画面 (`2`)**: 目の前の1タスク・タイマーだけに集中する超集中モード。
3. **デイリー画面 (`3`)**: 今日の全タスク・ハビットを俯瞰・連続消化・確認できるデイリービュー。
4. **マスター画面 (`4`)**: 全タスク・ハビットのバルク編集、ストリークソート、分析を行う管理ビュー。
5. **ビジョン画面 (`5`)**: 人生の長期目標・行動指針（クレド）の策定・確認ができるマニフェストビュー。
6. **タイマー画面 (`6`)**: ポモドーロや自由計測用の大型タイマービュー。

---

## 4. 📂 変更ファイル一覧
- `[MOD]` `js/config.js` : `APP_VERSION` 定義の追加
- `[MOD]` `index.html` : ヘッダーバージョンバッジおよびリリースノートモーダルDOMの追加
- `[MOD]` `style.css` : バージョンバッジおよびモーダルのスタイリング

---

## 5. 🧪 実施後のテスト・検証結果
- [x] PC版全画面（セクション・フォーカス・デイリー・マスター・ビジョン・タイマー）でのバージョンバッジ正常表示確認
- [x] バージョンバッジクリックによるリリースノートモーダル起動・ESC/オーバーレイクリックによるクローズ確認
- [x] UTF-8文字コード・Obsidian上での日本語レンダリング確認
