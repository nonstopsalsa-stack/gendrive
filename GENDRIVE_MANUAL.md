# ⚡ Gendrive 簡易操作マニュアル & ガイド

> **「意志力に頼らず、時間割と習慣の力で身体を自動で動かす」**  
> Gendrive（Habit Flow）の全体像と、今日から迷わず使いこなすためのクイックガイドです。

---

## 1. Gendriveの全体像（1分でわかるコンセプト）

Gendriveは、一般的な「ToDoリスト」や「習慣アプリ」とは違い、**「今日、どの時間帯に、何に集中するか」を脳の迷いゼロで実行する** ための実行特化型OSです。

```
┌─────────────────────────────────────────────────────────────┐
│ 【1日の流れ】                                               │
│   ① セクション（時間割）で今の枠を確認                      │
│   ② フォーカス（タイマー）で1つのタスクに超集中             │
│   ③ 完了を押すと自動で実績ログ・ストリーク（🔥）が蓄積     │
│   ④ スプレッドシート＆スマホと自動でミリ秒同期              │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 1日の基本ルーティン（どう使うか）

### 🌅 朝（1日のスタート）
1. Gendriveを開く（キーボードの **`1`** で「セクション画面」へ）。
2. **「第1セッション（早朝）」** に並んでいるルーチン・ハビットを確認し、上から順にスタート。
3. もし前日のやり残しがあれば、上部に表示される「📥 昨日までの未完了タスク」バナーから **「⚡ 現在のセクションに一括取り込み」** を押すだけで今日の予定に合流します。

### 💻 実行中（作業タイム）
1. やることを選んだら **`2`** を押して **「フォーカス画面」** に切り替える。
2. 画面が目の前のタスク1件だけになり、ライブタイマーが始動。
3. 終わったら **「✅ 完了」** を押す。実績時間（分）が自動記録され、次のタスクへ進みます。

### 🌙 夜・週末（振り返り）
1. **`4`** を押して **「マスター画面」** を開く。
2. ハビットの連続日数（🔥 **ストリーク**）や直近7日・28日の達成率、タイムラインログを眺めてニヤリとする。

---

## 3. 6つのメイン画面（数字キー `1`〜`6` で一発切替）

| キー | 画面名 | 役割と使い方 |
| :---: | :--- | :--- |
| **`1`** | **セクション** | **【基本画面】** 1日を「早朝・午前・午後・夜」の時間帯に分割した時間割ビュー。右上に「今日完了見込み時刻（🏁 ETA）」と時間経過バーが表示されます。 |
| **`2`** | **フォーカス** | **【超集中画面】** 余計な情報をすべて隠し、今やるべき1件とタイマーだけに集中する画面。 |
| **`3`** | **デイリー** | **【1日俯瞰】** 今日の全タスク・ハビットを縦断して一覧確認・進捗確認する画面。 |
| **`4`** | **マスター** | **【全体管理・分析】** Notionライクな表形式で、全タスク・ハビットの一括編集（バルクエディット）、ストリーク順ソート、達成率分析を行う画面。 |
| **`5`** | **ビジョン** | **【目標・マニフェスト】** 人生の長期目標や行動指針（クレド）を確認・整理する画面。 |
| **`6`** | **タイマー** | **【単独タイマー】** ポモドーロや自由計測用の大型タイマー画面。 |

---

## 4. これだけ覚えればプロ！神ショートカット

マウスを使わずにキーボードだけで爆速操作できます。

- **`1` 〜 `6`** ： 画面（ビュー）の切り替え
- **`T`** ： **🎯 タスク新規追加** モーダルを開く
- **`N`** ： **🌿 ハビット新規追加** モーダルを開く
- **`P`** ： **⚡ プリセットタスク**（よく使う定型作業の呼び出し）
- **`V`** ： 表示切り替え（すべて ➔ タスクのみ ➔ ハビットのみ）
- **`R`** ： データの即時再読み込み・クラウド再同期
- **`0`** または **`Home`** ： カレンダーを「今日」に一発リセット
- **`?`** ： ショートカット一覧ヘルプの表示

---

## 5. クラウド同期 & タイムマシン（バックアップ）の使い方

右上の **「☁️ Sync」** ボタンを押すと設定が開きます。

### 📱 スマホ（Android / PWA）との同期
- GASのウェブアプリURLを貼り付けて保存するだけで、PCで完了したタスクがスマホ側にも数秒で反映されます。

### ⏳ タイムマシン（Google Drive世代復元）
- **バックアップを取りたいとき**：
  - 「📤 Driveへ新規エクスポート」を押すと、Driveに日時付きの5シートスプレッドシート（`HABIT_EXPORT_...`）が自動保存されます。
- **過去の状態に戻したい / スプレッドシートから一括反映したいとき**：
  - 「🔄 世代一覧を取得」を押してプルダウンからファイルを選び、「⏪ 復元実行」を押すだけ。
  - ※Googleスプレッドシート上で30日分のログを追加したり、タスク名を一括置換したファイルからもそのまま復元可能です。

---

## 6. まとめ

- **迷ったらとりあえず `1`（セクション）を見る。**
- **作業するときは `2`（フォーカス）でタイマーを回す。**
- **データの編集・修正は `4`（マスター）か「Googleスプレッドシート」で行う。**

シンプルですが、このサイクルを回すだけで「生活リズムの崩れ」と「タスクの先延ばし」を完全に防ぐことができます。
